classdef class13
%	class13 - class of all output arguments
%	All the results (how good are evaluation method) will be collected here

properties(Access = public) 
    NumIm = 0;
    Ps_FM_Distances = 0;
    Alt_Ref_Distances = 0;
end

methods (Access = public)
    function WordDist = class13(NumIm, Ps_FM_Distances, Alt_Ref_Distances)
        % Calculation average word edit distance and print it
        if (nargin > 0)
            WordDist.PsFm
        end
    end    
end % public methods

end